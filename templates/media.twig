<body>
<div class="container">
    <div class="row no-gutter">
      {% for item in data %}
      <div class="col-sm-10">
          <a href = '{{ originelen }}{{ item.original }}'><img src = '{{ thumbs }}{{ item.thumb_large }}'></a>
      </div>
      <div class="col-sm-2">
        <h1>details</h1>
        <h2>Date taken</h2>
        {{ item.createddate }}
      </div>
      {% endfor %}
    </div>
</div>
</body>
